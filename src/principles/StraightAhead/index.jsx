// Straight Ahead and Pose to Pose — two approaches to building motion sequences.
// Straight Ahead: each frame flows from the last (continuous, unpredictable).
// Pose to Pose: key states are defined first, then interpolated (structured, controlled).
// In UI: Pose to Pose maps directly to keyframe/variant-based animation.
// Logic to be implemented.

export default function StraightAhead() {
  return null
}
