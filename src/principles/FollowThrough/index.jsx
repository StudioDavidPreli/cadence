// Follow Through and Overlapping Action — not everything stops at the same time.
// In UI: trailing elements that settle after the primary motion ends (e.g. list items, shadows).
// Logic to be implemented.

export default function FollowThrough() {
  return null
}
